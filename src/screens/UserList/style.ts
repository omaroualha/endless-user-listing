import {StyleSheet} from 'react-native';

export const Styles = StyleSheet.create({
  container: {
    marginTop: 32,
    paddingHorizontal: 24,
  },
  loader: {
    marginTop: 10,
    alignItems: 'center',
  },
});
